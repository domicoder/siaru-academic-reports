@model IEnumerable<SIARU.Application.DTOs.Professors.ProfessorListDto>

@{
    ViewData["Title"] = "Profesores";
}

<div class="page-header d-flex justify-content-between align-items-center flex-wrap gap-3">
    <div>
        <h1 class="page-title">Profesores</h1>
        <p class="page-subtitle">Gestiona los profesores registrados en el sistema.</p>
    </div>

    <a asp-action="Create" class="btn app-primary-button">Nuevo profesor</a>
</div>

<div class="table-card">
    <div class="table-responsive">
        <table class="table app-table align-middle mb-0">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Despacho</th>
                    <th>Área de conocimiento</th>
                    <th>Departamento</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="5" class="text-center py-4 text-muted">No hay profesores registrados.</td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Office</td>
                            <td>@item.KnowledgeAreaName</td>
                            <td>@item.DepartmentName</td>
                            <td class="text-end">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm app-table-button">Ver</a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm app-table-button">Editar</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm app-table-button-danger">Eliminar</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>