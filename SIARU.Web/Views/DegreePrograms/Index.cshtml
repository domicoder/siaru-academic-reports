@model IEnumerable<SIARU.Application.DTOs.DegreePrograms.DegreeProgramListDto>

@{
    ViewData["Title"] = "Titulaciones";
}

<div class="page-header d-flex justify-content-between align-items-center flex-wrap gap-3">
    <div>
        <h1 class="page-title">Titulaciones</h1>
        <p class="page-subtitle">Gestiona las titulaciones registradas en el sistema.</p>
    </div>

    <a asp-action="Create" class="btn app-primary-button">Nueva titulación</a>
</div>

<div class="table-card">
    <div class="table-responsive">
        <table class="table app-table align-middle mb-0">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="2" class="text-center py-4 text-muted">No hay titulaciones registradas.</td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td class="text-end">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm app-table-button">Ver</a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm app-table-button">Editar</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm app-table-button-danger">Eliminar</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>