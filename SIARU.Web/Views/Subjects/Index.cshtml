@model IEnumerable<SIARU.Application.DTOs.Subjects.SubjectListDto>

@{
    ViewData["Title"] = "Asignaturas";
}

<div class="page-header d-flex justify-content-between align-items-center flex-wrap gap-3">
    <div>
        <h1 class="page-title">Asignaturas</h1>
        <p class="page-subtitle">Gestiona las asignaturas registradas en el sistema.</p>
    </div>

    <a asp-action="Create" class="btn app-primary-button">Nueva asignatura</a>
</div>

<div class="table-card">
    <div class="table-responsive">
        <table class="table app-table align-middle mb-0">
            <thead>
                <tr>
                    <th>Código</th>
                    <th>Nombre</th>
                    <th>Curso</th>
                    <th>Área de conocimiento</th>
                    <th>Tipo</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="6" class="text-center py-4 text-muted">No hay asignaturas registradas.</td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Code</td>
                            <td>@item.Name</td>
                            <td>@item.Course</td>
                            <td>@item.KnowledgeAreaName</td>
                            <td>@item.Type</td>
                            <td class="text-end">
                                <a asp-action="Details" asp-route-id="@item.Code" class="btn btn-sm app-table-button">Ver</a>
                                <a asp-action="Edit" asp-route-id="@item.Code" class="btn btn-sm app-table-button">Editar</a>
                                <a asp-action="Delete" asp-route-id="@item.Code" class="btn btn-sm app-table-button-danger">Eliminar</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>