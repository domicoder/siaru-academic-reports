@model SIARU.Application.DTOs.Subjects.UpdateSubjectDto

@{
    ViewData["Title"] = "Editar asignatura";
}

<div class="page-header">
    <h1 class="page-title">Editar asignatura</h1>
    <p class="page-subtitle">Actualiza la información de la asignatura seleccionada.</p>
</div>

<div class="form-card">
    <form asp-action="Edit" method="post" novalidate>
        @Html.AntiForgeryToken()

        <div asp-validation-summary="ModelOnly" class="alert alert-danger app-alert mb-4"></div>

        <partial name="_SubjectForm" model="Model" />

        <div class="d-flex gap-2 flex-wrap mt-4">
            <button type="submit" class="btn app-primary-button">Guardar cambios</button>
            <a asp-action="Index" class="btn app-outline-button">Cancelar</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}